%table
  - if @users
    - @users.each do |user|
      %tr
        %td= user.email
        %td= user.approved
      - if user.approved == false
        %td= link_to 'Approve access', user_path(user), method: :put
  - else
    No users are awaiting to be approved
