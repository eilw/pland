.form-row
  .form-row-container
    .form-cell= SteelType.find(item.steel_type_id).steel_type

    .form-cell= SteelGrade.find(item.steel_grade_id).grade

    .form-cell= SteelFinish.find(item.steel_finish_id).finish

    .form-cell= SteelWidth.find(item.steel_width_id).width

    .form-cell= number_to_currency(item.price_kg)

    .form-cell= item.volume

    .form-cell= number_to_currency(item.cost)

    .form-cell.icon-cell
      = link_to '', edit_item_path(item), class: 'glyphicon glyphicon-edit'
      = link_to '', item_path(item), method: :delete, class: 'glyphicon glyphicon-remove'
